import SpriteKit

class MultiplayerArena: SKScene {
    // Properties
    private var armies: [ArmyManager] = []

    // Scene Lifecycle
    override func didMove(to view: SKView) {
        // Setup scene
    }

    // Game Loop
    override func update(_ currentTime: TimeInterval) {
        // Called before each frame is rendered
    }
}
